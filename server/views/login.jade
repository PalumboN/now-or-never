doctype
html
  head
    title Login  
  body
    script(type="text/javascript").
        console.log(!{JSON.stringify(user)});
        parent.postMessage(!{JSON.stringify(user)}, '*');
        window.close();

    p Welcom #{user.profile.displayName}
